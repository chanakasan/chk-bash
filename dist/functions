chk_bash_dist_path() {
  echo $chk_bash_dist_path
}

chk_edit() {
  local filename=$1
  vi `chk_bash_dist_path`/$filename
}

chk_source() {
  local filename=$1
  source `chk_bash_dist_path`/$filename
}

# vim:ft=sh:
